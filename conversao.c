#include "hush-02.h"

char	*valor_por_extenso(int num, t_dic dicio, int num_entradas)
{
	char	*resultado;
	int		centenas;

	resultado = (char *)malloc(sizeof(char) * 100);
	if (num >= 100)
	{
		
	}
}

/*OBS: é aqui onde é suposto ter a função que primordial do programa, a função que converte por exemplo:
123 para: cento e vinte três */
